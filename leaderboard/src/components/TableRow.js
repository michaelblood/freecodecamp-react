import React, { PropTypes } from 'react';

const TableRow = ({ camper }) => {
  return (
    <div>
      
    </div>
  );
};

TableRow.propTypes = {
  camper: PropTypes.object,
};

export default TableRow;
